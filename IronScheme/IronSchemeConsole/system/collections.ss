(library (system collections)
  (export
    hashtable:count
    hashtable:contains
    hashtable:containskey
    hashtable:item
    hashtable:new)
  (import
    (prefix (system collections hashtable) hashtable:))
)